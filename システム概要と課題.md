# 地方競馬分析システム — 概要と課題

最終更新: 2026-04-30 → [変更履歴](CHANGELOG.md)

---

## 🛠️ 開発ルール

**Pythonファイル修正後は必ず全テスト合格を確認すること。**

```bash
python -m pytest tests/ -v
```

- 全テスト `PASSED` になるまでコミット禁止
- 新機能追加・バグ修正時は `tests/` にテストも追加（現在11ケース）

---

## 🏗️ アーキテクチャ

### ファイル構成

```
地方競馬場/
├── index.html              フロントエンド（Bootstrap 5 + Chart.js、~3,700行、localStorage: keiba_v1）
├── server.py               ローカルAPIサーバー（Flask、http://127.0.0.1:5000）
├── tests/test_refund_parser.py
├── scripts/
│   ├── fetch_races.py      GitHub Actions: 当日データ収集
│   ├── fetch_history.py    手動: 過去データ遡り取得
│   ├── bot_runner.py       GitHub Actions: 仮想売買ボット
│   └── backfill_conditions.py  既存JSONへの条件補完（一回限り）
└── data/
    ├── YYYY-MM-DD.json     日別レース結果
    ├── bot_state.json / bot_settings.json
    ├── status.json         ヘルスチェック
    └── discovered_codes.json
```

### データフロー

```
keiba.go.jp
    ↓ fetch_races.py / fetch_history.py（GitHub Actions / 手動）
data/YYYY-MM-DD.json
    ↓ server.py（Flask API）
index.html（ブラウザ、localStorage保存）
    ↓ bot_runner.py（GitHub Actions）
data/bot_state.json → GitHub Pages公開
```

---

## 📡 データ収集

### 競馬場コード

| 競馬場 | コード | | 競馬場 | コード |
|---|---|---|---|---|
| 帯広（ばんえい） | 3 ✅ | | 園田 | 27 ✅ |
| 水沢 | 11 ✅ | | 門別 | 30 ⚠️ |
| 浦和 | 18 ✅ | | 高知 | 31 ✅ |
| 大井 | 20 ✅ | | 佐賀 | 32 ✅ |
| 金沢 | 22 ✅ | | 盛岡 | 35 ⚠️ |
| 笠松 | 23 ✅ | | 船橋 | 43 ⚠️ |
| 名古屋 | 24 ✅ | | 川崎 | 45 ⚠️ |
|  |  | | 姫路 | 51 ⚠️春秋のみ |

### APIエンドポイント（server.py）

| エンドポイント | 内容 |
|---|---|
| `GET /api/ping` | 死活確認 |
| `GET /api/schedule` | 本日の開催競馬場一覧 |
| `GET /api/races?venue=&date=` | レース番号一覧 |
| `GET /api/entries?venue=&date=&race=` | 出走馬（馬番・人気・オッズ・騎手・父・母父・調教師） |
| `GET /api/results?venue=&date=` | 全レース着順 |
| `GET /api/payouts?venue=&date=` | 払戻金（単勝/複勝/三連複等） |
| `GET /api/localdata/<date>` | data/YYYY-MM-DD.json をそのまま返す |
| `POST /api/batch_conditions` | 複数レースの条件を一括取得・JSON書き戻し |

### スクレイピング仕様・注意点

- **払戻パーサ**: キーワード検出方式（`table_idx==2` 廃止）→ 門別・大井・川崎など構造違いに対応
- **過去日付**: DebaTableは当日のみ。過去の条件取得は RefundMoneyList を使用
- **batch_conditions**: track または weather が欠けていれば必ず DebaTable フォールバック
- **正規化**: `_normalize(text)` で NFKC 変換（半角カナ・濁点合成・全角英数を統一）
- **帯広専用**: 馬場水分%（例: "1.6"）→ 良/稍重/重/不良 変換、surface は常に '直線'

---

## 🧠 予想エンジン

### スコアリング式

血統・調教師がある場合、既存5シグナルを85%に圧縮し残り15%を追加3シグナルに配分。

| 代表パターン | 配分 |
|---|---|
| オッズ＋騎手＋馬実績あり | 市場40% + 人気20% + 馬番12% + 騎手13% + 馬実績15% |
| オッズ＋馬実績あり | 市場44% + 人気23% + 馬番14% + 馬実績19% |
| オッズのみ | 市場50% + 人気30% + 馬番20% |
| どれもなし | 人気60% + 馬番40% |

追加3シグナル（最大15%・各6走以上必要）: 父6% / 母父4% / 調教師5%

### レース条件補正

| 条件 | 補正 |
|---|---|
| 重・不良 | 内枠+7%・外枠−7%・1人気−6%・6人気以下+5% |
| 稍重 | 内枠+3%・外枠−3% |
| 短距離(≤900m) | 内枠+5%・外枠−5% |
| 長距離(>1800m) | 高人気+3% |
| 少頭数(≤7頭) | 1人気+5% / 多頭数(≥14頭) 大外−5% |
| 馬体重変動 | ≥+10kg:×0.96 / ≤−10kg:×0.95 / ±6〜9kg:×0.98 |
| 父×馬場/距離適性 | 父の馬場別・距離別勝率比から ±5% |

### 確率計算

- **市場確率**: `mktProb(i) = (1/odds_i) / Σ(1/odds_j)`
- **騎手スコア**: τ=20レースの指数減衰加重（最低4エントリー）
- **馬実績スコア**: τ=90日の指数減衰加重（最低5走、ブレンド係数 = min(1, n/15)）
- **三連複**: Harville公式、9〜11頭→^0.92 / 12頭以上→^0.85 で大頭数補正

---

## 🤖 自動化・ボット

### GitHub Actions構成

| ワークフロー | 頻度 | 処理 |
|---|---|---|
| fetch_races | 1日最大10回（固定スケジュール） | 当日全レースを data/YYYY-MM-DD.json に保存 |
| bot_runner | レース後 | 着順確認→精算→bot_state.json 更新 |

### bot_runner.py 動作

1. `data/*.json` → `build_hist_stats()`（popStats/jockeyStats/horseStats/trainerStats/sireStats/bmsStats）
2. 本日の開催場・各レースをループ → `score_horses()` でスコアリング
3. `bot_decide()` でベット判断（Kelly or 固定額）
4. RefundMoneyList で着順確認 → 実払戻金で精算
5. `data/bot_state.json` に保存 → GitHub Pages 公開

---

## ⚠️ 課題・改善候補

| 優先 | 状態 | 内容 |
|---|---|---|
| 中 | 🔶 | 発走直前オッズ未取得（oddsUpdatedAt記録・変動検知済み、専用スクリプト未実装） |
| 中 | ⬜ | 単勝確定オッズをbacktest精算に未反映（`race.payouts.単勝` を runBacktest で使う） |
| 中 | 🟡 | 同厩舎・同産駒の相関をHarville公式が無視（大頭数補正で一部緩和） |
| 低 | ⬜ | localStorage→IndexedDB移行（5MB上限、ストレージメーター+削除ボタンで運用中） |
| 低 | ⬜ | GitHub Actionsスケジュール最適化（現在固定、最短5分間隔の制約） |
| 低 | ⬜ | keiba.go.jp HTML変更で全機能停止リスク（status.jsonで異常検出、修正は手動） |
| 低 | ⬜ | 予想モデル高度化（XGBoost等）・展開予想（逃げ/先行/差し/追込） |
